{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "crons": [
    {
      "comment": "Pull the calendar so Home is not showing yesterday. Daily, because the Hobby plan REJECTS the deployment outright for anything more frequent — it does not quietly downgrade it. 13:00 UTC is early morning Pacific, so the first look at Today is on fresh data; the refresh link on Home forces one in between.",
      "path": "/api/cron/sync-calendar",
      "schedule": "0 13 * * *"
    }
  ]
}
